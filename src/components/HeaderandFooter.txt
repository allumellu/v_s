import React from "react";
import { Link, BrowserRouter as Router, Switch, Route } from "react-router-dom";
import styled from "styled-components";
import Search from "./Search";
import AddProduct from "./Addproduct";
import Cart from "./Cart";
import Login from "./Login";
import Payment from "./Payment";
import Product from "./Product";
import ProductListing from "./ProductListing";
import Registration from "./Registration";
import Shop from "./Shop";

const HeaderWrapper = styled.header`
  background-color: #3f51b5;
  color: #fff;
  display: flex;
  justify-content: space-between;
  padding: 1rem;
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
`;

const Nav = styled.nav`
  display: flex;
  align-items: center;
  justify-content: flex-start;
`;

const NavItem = styled(Link)`
  color: #fff;
  margin-right: 1rem;
  text-decoration: none;

  &:hover {
    text-decoration: underline;
  }
`;

const Logo = styled.h1`
  font-size: 2rem;
  margin: 0;
`;

const FooterWrapper = styled.footer`
  background-color: #f2f2f2;
  border-top: 1px solid #ccc;
  display: flex;
  justify-content: space-between;
  padding: 1rem;
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 100;
`;

const Address = styled.p`
  margin: 0;
`;

const Phone = styled.p`
  margin: 0;
`;

const ChatButton = styled.button`
  background-color: #3f51b5;
  border: none;
  border-radius: 50%;
  color: #fff;
  font-size: 1.5rem;
  height: 50px;
  width: 50px;

  &:hover {
    cursor: pointer;
  }
`;

const Layout = ({ children }) => {
  return (
    <>
      <HeaderWrapper>
        <Nav>
          <NavItem to='/'>Home</NavItem>
          <NavItem to='/about'>About</NavItem>
          <NavItem to='/products'>Products</NavItem>
          <NavItem to='/shop'>Shop</NavItem>
          <NavItem to='/contact'>Contact</NavItem>
          <NavItem to='/login'>Login</NavItem>
        </Nav>
        <Search />
        <Logo>Virtual Shoppy</Logo>
      </HeaderWrapper>
      <Router>
        <Switch>
          <Route exact path='/'>
            {/* Home page component goes here */}
          </Route>
          <Route path='/about'>{/* About page component goes here */}</Route>
          <Route path='/products'>
            <ProductListing />
          </Route>
          <Route path='/shop'>
            <Shop />
          </Route>
          <Route path='/contact'>
            {/* Contact page component goes here */}
          </Route>
          <Route path='/login'>{/* Login page component goes here */}</Route>
        </Switch>
      </Router>
      <main>{children}</main>
      <FooterWrapper>
        <div>
          <Address>123 Main St</Address>
          <Address>Anytown, USA 12345</Address>
          <Phone>Customer Care: 1-800-555-5555</Phone>
        </div>
        <ChatButton>🤖</ChatButton>
      </FooterWrapper>
    </>
  );
};
export default Layout;
